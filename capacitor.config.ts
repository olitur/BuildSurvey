import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.neonpenguinwag',
  appName: 'neon-penguin-wag',
  webDir: 'dist'
};

export default config;
